import styles from "./Header.module.scss";

export default function HamburgerButton() {
    return (
        < div className={styles.HamburgerButton} id="hamburger_button">
        
        </div>

    )
}